# Foundry build artifacts
contracts/out/
contracts/cache 
contracts/broadcast

# Any forge-installed libs that are not registered submodules
contracts/lib/

# Environment — contains private keys and RPC URLs
contracts/.env